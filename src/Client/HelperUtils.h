#include <string>
#include <windows.h>
#include <algorithm>
#include <cctype> 

class HelperUtils {

public:
    static std::string toLowerCase(std::string& text);

    //static std::string readDataFromJson(const std::wstring& configFile, std::string first, std::string second);
};
